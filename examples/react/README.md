# haribote + React example

## Node.js

```bash
# development server (Vite)
pnpm dev

# build → start Node.js server
pnpm build
pnpm serve
```

## Cloudflare Workers

```bash
# build → local preview
pnpm build
pnpm dev:cf

# deploy
pnpm wrangler deploy
```
